import * as React from 'react';
import MainContainer from './navigation/MainContainer';
import 'expo-dev-client';

function App(){
  return(
    <MainContainer>

    </MainContainer>
  );
}

export default App;